// Laço while executa enquanto a condição for verdadeira
// Declarando variavel indice
var indice = 1;
// Console.log do while
while (indice < 51) {
  if (indice % 3 == 0)  {
    console.log("pi");
  }
  else {
    indice % 3 == 0
  }
  // Escreve no log do navegador
  console.log(indice);
  // Incrementar o índice
  indice = indice + 1;
}
